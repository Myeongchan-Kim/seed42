# 알게 된 것

LLM 연상망에서 두 단어 사이 거리를 재는 실험. 측정값에는 얻은 조건과 검증 방법을
같이 적는다. **기각된 결과도 남긴다** — 왜 틀렸는지가 다음 버그의 단서다.

현재 그래프: `gemini-3.7-flash` seed 42, 파이프라인 v4, 노드 98,684 / 엣지 1,333,717
(그중 실제로 던져본 노드 18,721)

마지막 갱신: 2026-08-30

---

## 1. 재현성은 seed 가 준다. temperature 는 아니다

`gemini-flash-lite-latest`, 같은 프롬프트 5회 반복:

| 조건 | 서로 다른 응답 |
|---|---|
| T=1.0, seed 없음 | 5/5 |
| **T=0.0, seed 없음** | **5/5** |
| T=0.0, seed=42 | 1/5 |
| T=1.0, seed=42 | 1/5 |

**temperature=0 만으로는 재현성이 전혀 없다.** 그리고 seed 를 고정하면 temperature 는
아예 무효다 — seed=7 에서 T=0 / 1.0 / 2.0 이 모두 같은 응답(1541자, 동일 해시)을 냈다.
seed 를 바꾸면(1/2/3) 응답이 달라지므로 seed 가 실제로 샘플링을 지배한다.

그래서 `graph_config` 의 정체성에 들어가는 것은 temperature 가 아니라 **seed** 다.

참고로 웹 서비스(ChatGPT·Gemini 웹앱·Claude.ai)의 파라미터는 공개되지 않는다.
확인 가능한 것은 API 기본값뿐이다 — Gemini 전 모델이 `temperature 1.0 / top_p 0.95 /
top_k 64`, OpenAI·Anthropic 도 기본 1.0.

## 2. 원래 질문의 답: 발산이냐 그룹핑이냐는 잘못된 이분법

던져본 노드 18,721 개의 유도 부분그래프에서:

    군집계수      0.137
    밀도          0.00295     -> 무작위 대비 46배 뭉침
    최대강연결성분 15,354 (82%) -> 그런데도 서로 도달 가능
    상호성        0.068

뭉쳐 있으면서 동시에 잘 연결돼 있다. 그것이 small-world 의 정의다.

**측정 주의 1**: 반드시 *던져본* 노드만으로 재야 한다. 전체 그래프로 재면 이웃으로만
등장한 프론티어 노드(80%)가 분모를 채우는데, 그것들은 나가는 엣지가 있을 수 없다.

**측정 주의 2**: 표본이 작으면 값이 크게 틀어진다. 189 노드에서는 군집계수 0.277 /
상호성 0.198 / SCC 65% 였다. 100 배 키우니 군집계수와 상호성은 절반 이하로 떨어지고
SCC 는 82% 로 올랐다. **초기 수치를 결론으로 쓰면 안 된다.**

## 3. 연상 그래프는 강하게 방향성이 있다

던져본 노드 사이 **상호성 0.068** — 연상의 93% 가 일방통행이다.

초기 양방향 탐색은 목표어 쪽 엣지를 뒤집어 썼는데, 검증해보니 **뒤집은 엣지 12개 중
순방향으로도 성립한 것은 1개**뿐이었다.

    X  잔혹 → 베르세르크        (베르세르크 → 잔혹 은 성립)
    O  계획 → 구체적

"A 를 던지면 B 가 나온다"에서 "B 를 던지면 A 가 나온다"를 끌어낼 수 없다.

## 4. 언어 간 다리는 거의 완전한 일방통행

|  | 엣지 수 |
|---|---|
| `ko → en` | 32,538 |
| `en → ko` | **33** |
| 비율 | **986 : 1** |

한국어 단어를 던지면 응답에 영어 용어가 쏟아진다(`미토콘드리아` → `ATP`, `membrane`).
영어 단어를 던지면 한국어는 사실상 나오지 않는다.

그 결과가 그래프 전체를 바꿔놨다. **시드가 전부 한국어였는데 엣지의 62% 가 `en → en`
이다.** 크롤러가 한국어 응답에 섞인 영어 용어로 넘어간 뒤 돌아오지 못한 채 영어 영역만
넓혔다.

이 값은 옛 토크나이저(정규식)에서 1,097:1, 새 토크나이저(형태소 분석기)에서 986:1 로
거의 같다 — **측정 도구의 아티팩트가 아니다.**

### 영어로 끌리는 정도는 언어마다 다르다

같은 개념을 4개 언어로 번역해 던지고 응답의 라틴 문자 비율을 쟀다:

| 시드 언어 | 라틴 문자 비율 | 응답당 영어 용어 |
|---|---|---|
| 중국어 | 16.4% | 19.6종 |
| 한국어 | 15.3% | 15.1종 |
| **일본어** | **3.4%** | **3.1종** |

**일본어가 5배 가까이 덜 끌려간다.** 한국어·중국어는 `ATP`·`membrane`·`democracy`·
`kratos` 를 그대로 노출하는데, 일본어는 `酸化的リン酸化` 처럼 자국어로 옮겨 쓴다.
가타카나로 외래어를 흡수하는 관행이 오히려 라틴 문자 유입을 막는 것으로 보인다.

스페인어는 라틴 문자를 영어와 공유해 이 지표로는 못 잰다.

## 5. LLM 은 자기 연상 구조를 45% 만 안다

"어떤 단어를 던지면 X 가 나오나?" 의 답을 실제로 던져 검증한 결과 (`introspect.py`):

| 대상 | 내성 정확도 | |
|---|---|---|
| 커피 | 6/8 (75%) | 흔함 |
| 재즈 | 5/8 (63%) | 흔함 |
| 미토콘드리아 | 5/8 (63%) | 흔한 전문어 |
| 베르세르크 | 2/8 (25%) | 니치 |
| 숙신산 | 0/8 (0%) | 니치 + 동음이의 |
| **전체** | **18/40 (45%)** | |

**내성 정확도가 대상의 대중성을 따라간다.** 니치한 대상에 대해서는 그럴듯하지만
성립하지 않는 선행어를 지어낸다. `중세 유럽`·`검과 마법`·`복수 이야기` 는 베르세르크를
설명하는 말로는 완벽하지만 실제로 던지면 베르세르크가 나오지 않는다.

**함의**: LLM 에게 "이걸 어떻게 찾지?" 를 물어 검색 전략을 짜게 하는 설계는 대상이
니치할수록 듣지 않는다.

## 6. 클러스터 탈출은 의미가 아니라 중의성으로 일어난다

맥락 없는 단어는 중의적이고, LLM 이 어느 쪽으로 읽느냐가 사슬의 운명을 정한다.

| 단어 | 의도 | LLM 이 읽은 것 | 결과 |
|---|---|---|---|
| 숙신산 | succinic acid | **肅愼山** (고대 북방 민족) | 생화학 → 사기·주나라·법가 |
| 마력 | horsepower | **魔力** | 물리 → 판타지 |
| 광전사 | berserker | **光電**효과 | 예상한 다리가 없었다 |

의미적 연상이 아니라 **어휘적 우연**이 클러스터 사이의 다리가 된다.

가장 선명한 것은 **숙신산 0/8**. 유기산·발효·식품첨가물 — 생화학적으로 전부 맞는
선행어인데 하나도 성립하지 않는다. **중의성은 다른 클러스터로 나가는 다리를 만드는
동시에, 자기 클러스터에서 들어오는 다리를 전부 끊는다.**

다만 이것은 **모델 고유의 성질이기도 하다**. `gemini-3.7-flash` 는 숙신산을 호박산
(succinic acid)으로 정확히 읽는다. gpt-4.1-mini 의 오독이었다.

## 7. 도구 선택은 실측으로 정해야 했다

한국어 조사를 정규식으로 떼다가 다섯 번 넘게 땜질했다 (`말부터`→`말`, `효과`가 `효`로,
`은하`가 용언 어간으로 오인). 형태소 분석기를 쓰면 "명사만" 이라고 말하면 되므로 그
규칙들이 통째로 사라진다. 그런데 **어느 분석기인지는 언어마다 달랐다**:

| 언어 | 선택 | 탈락 이유 |
|---|---|---|
| ko | `kiwipiepy` | spaCy 가 `미토+콘드리아+는` 처럼 형태소를 `+` 로 이어 붙인다 |
| zh | `jieba` | spaCy 가 `线粒体`를 `线`+`粒体`로 자르고 `细胞`를 동사로 태깅 |
| ja / es / en | spaCy | 양호 |

임베딩도 마찬가지다. 짧은 단어에서 척도가 압축돼 **절대 임계값을 쓸 수 없다**:

| 쌍 | OpenAI | Gemini |
|---|---|---|
| `미토콘드리아` ~ `mitochondria` (같은 것) | 0.225 | 0.825 |
| `ATP` ~ `아데노신삼인산` (같은 것) | 0.111 | 0.698 |
| `재즈` ~ `블루스` (다름) | 0.259 | 0.697 |
| `ATP` ~ `NADH` (다름) | 0.505 | 0.617 |

Gemini 는 교차언어를 제대로 잡지만(0.22 → 0.83) 분포는 여전히 겹친다
(같은 개념 최저 0.698 < 다른 개념 최고 0.706). 동일성 판정은 임베딩이 아니라 LLM 심판이,
언어 판정은 문자 범위가 맡는다.

---

## 현재 측정값

    MC number(TCA cycle, 베르세르크) = 8홉   [v4 그래프 BFS, 검증 8/8 통과]

    TCA cycle → respiration → Fermentation → Kimchi → 김치 → 식품 → 세계 → 판타지 → 베르세르크

`Kimchi → 김치` 가 그 희귀한 영어→한국어 다리다. 986:1 로만 존재하는 33개 중 하나이고,
한국어 차용어라서 가능했다.

무방향으로 풀면 3홉(`TCA cycle → General → 특정 → 베르세르크`)이 나오지만 **검증 1/3 로
기각된다** — 총칭 허브를 지나고 엣지를 뒤집어야만 성립하는 가짜 지름길이다.

**여전히 상한이다.** 그래프에 없는 엣지는 세지 못하므로 크롤을 더 하면 짧아질 수 있다.

### 연결성과 탐색가능성은 다르다

8홉 경로는 처음부터 존재했지만 온라인 탐색으로는 찾지 못했다. 목표어를 아는 정책조차
수십 노드를 열고 실패했다. `Fermentation → Kimchi` 같은 도약은 국소 정보로 예측할 수
없기 때문이다. 밀그램이 말한 연결성(짧은 경로가 존재하는가)과 탐색가능성(찾아낼 수
있는가)의 차이 그대로다. **관측을 쌓은 뒤 BFS 로 재는 것이 실제 거리에 가깝다.**

---

## 기각된 결과 (전부 그럴듯해 보였다)

이 실험의 반복되는 패턴: **틀린 결과가 언제나 그럴듯해 보인다.** 연상 사슬은 원래
느슨해서 잘못된 경로도 읽으면 말이 된다. 눈으로 검수하는 것은 통하지 않았고, 매번
기계적 검증을 붙여야만 걸러졌다.

| 잘못된 결과 | 원인 |
|---|---|
| 도달 판정 전부 실패 | 임계값 0.86 — 애초에 도달 불가능한 값 |
| 개념 동일성 판정 실패 | 임베딩 코사인: 같은 개념 0.11 < 다른 개념 0.51 |
| `MC number ≤ 2` | 판정 프롬프트의 라벨("목표")과 후보 단어가 충돌 |
| `... → 애니메이션` 4홉 | 상위 범주를 같은 개념으로 판정 |
| `... → 스트레스 → 수면 → 스트레스` 8홉 | `None` 이 "목표어"와 "실패" 두 뜻 — 자료구조 오염 |
| 상호성 0.022 | 프론티어 노드가 분모를 채워 희석 |
| `database is locked` 수정 2회 실패 | 오류 0건을 성공으로 읽음 — 실제로는 조용히 멈춰 있었다 |

마지막 항목이 가장 위험한 종류다. **"적용했다"고 출력하는 것과 실제로 적용된 것은
다르다.** 보고 전에 산출물을 직접 확인해야 한다.

### 판정기는 넓게 묻는 것과 좁게 묻는 것이 다르다

후보 12개를 한 번에 묻는 배치 판정은 재현율이 높지만 상위 범주를 통과시킨다
(베르세르크는 애니메이션이니까). 같은 모델도 짝 단위로 물으면 정확하다.

    same_concept(애니메이션, 베르세르크)     False
    same_concept(광전사, 베르세르크)         False
    same_concept(Berserk, 베르세르크)        True

**넓게 물어 후보를 좁히고, 좁게 물어 확정하라.**
