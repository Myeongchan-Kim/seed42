# 알게 된 것

`TCA cycle ↔ 베르세르크` 를 잇는 실험에서 나온 것들. 1~6 은 gpt-4.1-mini (prompt v2) 관측이고,
0 번대는 Gemini 전환(prompt v3)에서 나온 것이다. 측정값에는 얻은 조건과
검증 방법을 같이 적는다. **기각된 결과도 남긴다** — 왜 틀렸는지가 다음 버그의 단서다.

마지막 갱신: 2026-08-30 (Gemini 전환)

---

## 0. 재현성은 seed 가 준다. temperature 는 아니다

`gemini-flash-lite-latest`, 같은 프롬프트("미토콘드리아") 5회 반복:

| 조건 | 서로 다른 응답 |
|---|---|
| T=1.0, seed 없음 | 5/5 |
| **T=0.0, seed 없음** | **5/5** |
| T=0.0, seed=42 | 1/5 |
| T=1.0, seed=42 | 1/5 |

**temperature=0 만으로는 재현성이 전혀 없다.** 그리고 seed 를 고정하면 temperature 는
아예 무효다 — seed=7 에서 T=0 / 1.0 / 2.0 이 모두 동일한 응답(1541자, 같은 해시)을 냈다.
seed 를 바꾸면(1/2/3) 응답이 달라지므로 seed 가 실제로 샘플링을 지배한다.

그래서 `graph_config` 의 정체성에 들어가는 것은 temperature 가 아니라 **seed** 다.
temperature 는 기록만 한다.

덧붙여, 우리에게는 이미 더 강한 보장이 있다 — **캐시**. 응답이 `(모델, 단어, seed)` 로
DB 에 저장되므로 그래프는 한 번 샘플링되면 고정된다. seed 가 주는 추가 가치는 캐시 없이
처음부터 다시 돌리는 사람이 같은 그래프를 얻는다는 것이다.

## 0b. 임베딩: Gemini 는 교차언어를 인식한다 (그래도 임계값은 못 쓴다)

| 쌍 | OpenAI | Gemini |
|---|---|---|
| `미토콘드리아` ~ `mitochondria` (같은 것) | 0.225 | **0.825** |
| `재즈` ~ `jazz` (같은 것) | 0.315 | **0.816** |
| `ATP` ~ `아데노신삼인산` (같은 것) | 0.111 | 0.698 |
| `재즈` ~ `블루스` (다름) | 0.259 | 0.697 |
| `시트르산` ~ `숙신산` (다름) | 0.481 | 0.706 |
| **같은 개념 최저 / 다른 개념 최고** | 0.111 / 0.505 | 0.698 / 0.706 |

OpenAI 에서는 번역쌍이 무관한 단어보다도 낮게 나왔다(완전한 실패). Gemini 는 교차언어를
제대로 잡는다. 하지만 분포는 여전히 아슬아슬하게 겹쳐서 **절대 임계값은 못 쓴다**.
구조는 그대로 유지한다 — 동일성은 `canonical` 정규화, 도달 판정은 LLM 2단 게이트,
코사인은 순위용. 다만 순위 신호가 훨씬 믿을 만해졌으므로 `blind` 정책이 개선될 것이다.

## 0c. 쿼리 단어는 띄어쓰기 없는 한 단어 명사만

조합어를 허용하면 노드가 폭발한다. k=40 목록에서 `에너지` / `에너지 생성` / `생성` 이
서로 다른 노드가 되어 한 개념이 쪼개졌고, `저장`·`조절`·`저장 공간` 같은 잡음이 늘었다.

**대가**: `미우라 켄타로` 같은 띄어쓰기 있는 고유명사가 배제된다. 그런데 그것이 바로
베르세르크로 들어가는 유일한 진입 엣지였다(2번 참조). **니치한 대상은 더 도달하기
어려워진다.** 조합어 폭발을 막는 이득이 더 크다고 보고 채택했지만, MC number 값을
비교할 때 이 규칙(prompt_version v3)이 다르면 비교가 성립하지 않는다.

## 0d. 중의성 다리는 부분적으로 모델 고유의 성질이다

`숙신산` 을 gpt-4.1-mini 는 肅愼山(고대 북방 민족)으로 읽었고, 그것이 생화학 클러스터를
탈출하는 다리가 됐다(5번). **gemini-3.7-flash 는 호박산(succinic acid)으로 정확히 읽는다.**

    gemini  숙신산 → 호박산, 디카르복실산, 미토콘드리아, 푸마르산, ATP, 감칠맛, 발효

따라서 5번의 관측들은 "LLM 일반의 성질" 이 아니라 적어도 부분적으로 **gpt-4.1-mini 의
성질**이다. 같은 seed·같은 K 로 두 모델을 비교하는 것이 다음 실험이다.

---

## 0e. 웹 서비스의 temperature 는 공개되지 않는다

ChatGPT·Gemini 웹앱·Claude.ai 모두 샘플링 파라미터를 공개하지 않는다. 확인 가능한 것은
API 기본값뿐이다 (모델 메타데이터 조회):

    gemini-3.7-flash / 3.5-flash / 2.5-flash / flash-lite-latest
      temperature 1.0 (최대 2.0)   top_p 0.95   top_k 64

OpenAI·Anthropic API 기본값도 temperature 1.0 이다. 웹 제품이 0 을 쓰지 않는 것은
확실하다 - 같은 질문에 답이 달라지므로.

**이것이 연구 방향과 충돌한다.** 재현성을 위해 seed 를 고정하면 그래프가 하나로 고정되지만,
실제 사용자가 겪는 것은 seed 없이 샘플링된 응답이다. "AI 가 사람들을 연결하는가 단절하는가"
같은 질문이라면 측정 대상은 *하나의 그래프*가 아니라 **그래프의 분포**여야 한다.
seed 를 1..N 으로 돌려 N 개 그래프를 만들고 분산을 보는 설계가 필요하다.
seed 는 이미 `graph_config` 에 들어 있으므로 그대로 가능하다.

## 1. 원래 질문의 답: 둘 다다

> "인간관계처럼 발산해서 잘 연결될지, 그룹핑이 심하게 되어서 다른 topic으로 잘 안넘어가는지"

**잘못된 이분법이었다.** 확장된 노드 189개의 유도 부분그래프에서 (k=40):

    군집계수     0.277
    밀도         0.021      -> 무작위 대비 13배 뭉침
    최대강연결성분 122/189 (65%)  -> 그런데도 서로 도달 가능
    상호성       0.198

뭉쳐 있으면서 동시에 잘 연결돼 있다. 그것이 small-world 의 정의다.
"인간관계처럼" 이라는 처음 직관이 맞았고, 다만 둘 중 하나가 아니라 둘 다가 답이었다.

**측정 주의**: 반드시 *확장된* 노드만으로 재야 한다. 전체 그래프(4,049노드)로 재면
군집계수 0.080 / 상호성 0.022 로 나오는데, 95% 가 이웃으로만 등장한 프론티어 노드라
나가는 엣지가 있을 수 없어 분모만 채운다. 상호성은 한 자릿수 배가 틀어진다.

## 2. MC number 는 K 의 함수다

K(노드당 이웃 수)는 구현 세부사항이 아니라 **정의의 일부**다.

    k=12   다크 판타지 만화 → 베르세르크   X
    k=40   다크 판타지 만화 → 베르세르크   O

K 가 작으면 그래프는 **유명세 그래프**가 된다. 각 단어의 가장 유명한 이웃만 살아남고
니치한 대상은 진입차수가 0 이 되어 구조적으로 도달 불가능해진다. `일본 만화 명작` 을
던지면 드래곤볼·원피스는 나와도 베르세르크는 안 나온다.

`베르세르크` 의 진입차수는 사실상 고유명사 둘뿐이다.

    미우라 켄타로 → 베르세르크   O   (작가)
    가츠         → 베르세르크   O   (주인공)
    다크 판타지 만화 → X    일본 만화 명작 → X    흑검사 → X    광전사 → X

**어려운 것은 생화학에서 만화로 건너가는 게 아니라 마지막 한 홉이다.** 밀그램 실험과
같은 구조 — 대상의 도시·직업까지는 쉽고, 그 사람 본인에게 닿는 것이 어렵다.

**K 를 명시하지 않은 MC number 는 무의미하다.**

### K 사이의 관계

    k=12 집합 ⊂ k=40 집합 :  12/12   (포함은 성립)
    k=12  vs  k=40 앞 12개 :  65%    (순서는 다름)

포함은 되지만 순위가 재현되지 않는다. 그래서 `rank <= k` 로 작은 K 그래프를 유도하는
저장 최적화는 쓸 수 없고, K 마다 별도 추출을 저장해야 한다.

## 3. 연상 그래프는 강하게 방향성이 있다

확장된 노드 사이 **상호성 0.198** — 연상의 80% 가 일방통행이다.

초기 양방향 탐색은 목표어 쪽 엣지를 뒤집어 썼는데, 검증해보니 **뒤집은 엣지 12개 중
순방향으로도 성립한 것은 1개**뿐이었다.

    X  잔혹 → 베르세르크     (베르세르크 → 잔혹 은 성립)
    X  욕망 → 베르세르크
    O  계획 → 구체적

"A 를 던지면 B 가 나온다"에서 "B 를 던지면 A 가 나온다"를 끌어낼 수 없다. 그래서
역방향 탐색은 `llm.predecessors` 로 선행어를 제안받고 실제 확장으로 검증한다.

## 4. LLM 은 자기 연상 구조를 45% 만 안다

"어떤 단어를 던지면 X 가 나오나?" 의 답을 실제로 던져 검증한 결과 (`introspect.py`, k=40):

| 대상 | 내성 정확도 | |
|---|---|---|
| 커피 | 6/8 (75%) | 흔함 |
| 재즈 | 5/8 (63%) | 흔함 |
| 미토콘드리아 | 5/8 (63%) | 흔한 전문어 |
| 베르세르크 | 2/8 (25%) | 니치 |
| 숙신산 | 0/8 (0%) | 니치 + 동음이의 |
| **전체** | **18/40 (45%)** | |

**내성 정확도가 대상의 대중성을 따라간다.** 니치한 대상에 대해서는 그럴듯하지만
성립하지 않는 선행어를 지어낸다. `중세 유럽`·`검과 마법`·`복수 이야기` 는 베르세르크를
설명하는 말로는 완벽하지만 실제로 던지면 베르세르크가 나오지 않는다.

이것이 `aware` 정책과 역방향 유역이 실패하는 이유다. 확장 51회 중 39회가 경로
검증이었고 전부 기각됐다 — 유역이 대체로 허구였다.

**함의**: LLM 에게 "이걸 어떻게 찾지?" 를 물어 검색 전략을 짜게 하는 설계는 대상이
니치할수록 듣지 않는다.

## 5. 클러스터 탈출은 의미가 아니라 중의성으로 일어난다 (4회 관측)

맥락 없는 단어는 중의적이고, LLM 이 어느 쪽으로 읽느냐가 사슬의 운명을 정한다.

| 단어 | 의도 | LLM 이 읽은 것 | 결과 |
|---|---|---|---|
| 숙신산 | succinic acid | **肅愼山** (고대 북방 민족) | 생화학 → 사기·주나라·법가 |
| 마력 | horsepower | **魔力** | 물리 → 판타지 |
| 광전사 | berserker | **光電**효과 | 예상한 다리가 없었다 |
| 크리스타 | cristae | 다른 것 | 미토콘드리아 → 애니메이션 |

의미적 연상이 아니라 **어휘적 우연**이 클러스터 사이의 다리가 된다. 그라노베터의
약한 연결에 해당하지만 출처가 다르므로 두 종류의 다리는 따로 세어야 한다.

가장 선명한 것은 **숙신산 0/8**. 유기산·발효·식품첨가물 — 생화학적으로 전부 맞는
선행어인데 하나도 성립하지 않는다. **중의성은 다른 클러스터로 나가는 다리를 만드는
동시에, 자기 클러스터에서 들어오는 다리를 전부 끊는다.**

## 6. 총칭어 허브는 상투구에서 나온다

맨 단어를 던지면 LLM 은 흔히 "더 구체적인 맥락을 알려주시면 안내해 드리겠습니다" 를
붙인다. 추출기가 본문이 아니라 **그 상투구를 캐면** 종류·계획·구체적·안내 같은 내용
없는 단어가 쏟아지고, 그래프가 의미 없는 지름길로 가짜 small-world 가 된다.

    ... 세제 → 종류 → 계획 → 구체적 → ...   (연상 사슬이 아니라 잡음)

추출 프롬프트에서 상투구와 총칭어를 금지하고, `llm.is_generic` 안전망을 뒀다.
그래도 `문화`·`표현`·`감정`·`주제` 는 여전히 상위 허브로 샌다 (k=40 기준 진입차수 18~29).
경계가 모호한 것들이라 미해결로 남겨둔다.

---

## 현재 측정값

    MC_12(TCA cycle, 베르세르크) : 도달 불가 (진입차수 0 — 마지막 홉이 없음)
    MC_40(TCA cycle, 베르세르크) : 미확정 (양방향, 예산 50, 세 정책 모두 실패)
    MC_12(재즈, 커피)            : 미확정 (양방향, 예산 20, blind)

찾은 것 중 가장 그럴듯한 다리 (검증 전 버전이라 최종 결과로 치지 않는다):

    TCA 회로 → 에너지 → 동력 → 마력 → 판타지 → 마법 세계 → ...

---

## 기각된 결과 (전부 그럴듯해 보였다)

이 실험의 반복되는 패턴: **틀린 결과가 언제나 그럴듯해 보인다.** 연상 사슬은 원래
느슨해서 잘못된 경로도 읽으면 말이 된다. 눈으로 검수하는 것은 통하지 않았고, 매번
기계적 검증을 붙여야만 걸러졌다.

| # | 잘못된 결과 | 원인 |
|---|---|---|
| 1 | "3정책 전부 실패" | 도달 임계값 0.86 — 애초에 도달 불가능한 값 |
| 2 | 개념 동일성 판정 실패 | 임베딩 코사인: 같은 개념 0.11 < 다른 개념 0.51 |
| 3 | "LLM 은 클러스터를 못 벗어난다" | 레벨 beam 이 찾아낸 다리(`조명`)를 버림 |
| 4 | `MC number ≤ 2` | 배치 판정 거짓 양성 + 프롬프트 라벨 충돌("목표") |
| 5 | `... → 애니메이션` 4홉 | 상위 범주를 같은 개념으로 판정 |
| 6 | `... → 스트레스 → 수면 → 스트레스` 8홉 | `None` 이 "목표어"와 "실패" 두 뜻 — 유역 오염 |
| 7 | 상호성 0.022 | 프론티어 노드가 분모를 채워 희석 |

### 임베딩은 도달 판정에 쓸 수 없다

짧은 단어에서 척도가 압축돼 같은 개념과 다른 개념의 분포가 완전히 겹친다.

| 쌍 | 코사인 | |
|---|---|---|
| `ATP` ~ `아데노신 삼인산` | 0.111 | 같은 것 |
| `cellular respiration` ~ `세포 호흡` | 0.120 | 같은 것 |
| `재즈` ~ `블루스` | 0.259 | 다름 |
| `citric acid cycle` ~ `TCA 싸이클` | 0.419 | 같은 것 |
| `ATP` ~ `NADH` | 0.505 | 다름 |

임계값이 존재하지 않는다. 역할을 셋으로 나눴다 — 순위는 코사인, 개념 동일성은
`llm.canonical` 정규화, 도달 판정은 `llm.find_target` + `llm.same_concept` 2단 게이트.

### 판정기는 넓게 묻는 것과 좁게 묻는 것이 다르다

후보 12개를 한 번에 묻는 배치 판정은 재현율이 높지만 상위 범주를 통과시킨다
(베르세르크는 애니메이션이니까). 같은 모델도 짝 단위로 물으면 정확하다.

    same_concept(애니메이션, 베르세르크)     False
    same_concept(광전사, 베르세르크)         False
    same_concept(켄타로 미우라, 베르세르크)  False
    same_concept(Berserk, 베르세르크)        True

**넓게 물어 후보를 좁히고, 좁게 물어 확정하라.**
