# 비밀
.env

# 파생 데이터 — 코드로 다시 만들 수 있고 용량이 크다.
# number.db 는 관측 원장 + 그래프 (수백 MB). crawl/retokenize 로 재생성한다.
number.db
number.db-shm
number.db-wal
# seeds_live.py 가 매번 새로 만든다. 그날의 화제라 어차피 변한다.
seeds_live.txt
viewer.html

# 로그
*.log

# 파이썬
.venv/
__pycache__/
*.pyc
