# 형태소 분석기는 버전을 고정한다 - 바뀌면 토큰이 달라져 그래프가 달라진다.
# (number/tokens.py 참조: ko=kiwipiepy, zh=jieba, ja/es/en=spacy)
spacy==3.8.16
kiwipiepy==0.23.2
jieba==0.42.1

# 나머지는 파이썬 버전에 맞게 풀어 둔다. 로컬은 3.12, 서버는 3.11 이라
# 정확히 고정하면 한쪽에서 설치가 안 된다.
google-genai>=2.20
openai>=3.0
python-dotenv>=1.2
truststore>=0.10
numpy>=2.0,<3
networkx>=3.4
flask>=3.1
waitress>=3.0
markdown>=3.7
